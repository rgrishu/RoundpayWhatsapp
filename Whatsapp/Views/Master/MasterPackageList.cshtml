
<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title">
            Master Package 
        </h3>
        
    </div>

    <div class="row">
        <div class="col-12 grid-margin">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-2">
                            <h4 class="card-title">

                                <i class="fas fa-wrench"></i>
                                 Service  List

                            </h4>
                        </div>
                        <div class="col-sm-10">
                          <button id="btncreateuser" class="btn btn-primary btn-sm float-right"
                        type="submit">
                     + Create New
                </button>
                        </div>
                    </div>



                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Package Name</th>
                                    <th>Cost</th>
                                    <th>Validity In Days</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>







</div>

@section scripts{
<script>
    $(document).ready(function () {
        loadData();
    });
    function loadData(){
         $.ajax({
               type: 'get',
                url: '/Master/GetMasterPackageList',
                success: function (data) {
                      $('tbody').html(data);
                },
                error: function (data) {
                    console.log('An error occurred.');
                    console.log(data);
                },
          });
    }
    $('#btncreateuser').click(function () {
           RenderForm(0);
        });
    function RenderForm(id){
        $.ajax({
                type: 'post',
                url: '/Master/CreateMasterPackage/',
                data: { id:id},
                success: function (data) {
                    Q.alert({
                        title: "Add Service",
                        body: data,
                        width: '500px',
                    });
                },
                error: function (data) {
                },
            });
    }
</script>
}
